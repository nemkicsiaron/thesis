datasource db {
    provider = "sqlite"
    url      = "file:./test.db"
}

model Post {
    title String
    category String 
    published Boolean? @default(false)
    description String?
    Price String
    created DateTime
    author String
    signature String @unique
}