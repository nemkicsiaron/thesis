{
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "start": "node --experimental-fetch --experimental-specifier-resolution=node --experimental-modules ./build",
    "predemon": "npm run build",
    "demon": "nodemon --experimental-fetch --experimental-specifier-resolution=node --experimental-modules ./build"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.17",
    "prisma": "^3.14.0",
    "ts-node": "^10.5.0",
    "tslint": "^6.1.3",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@prisma/client": "^3.14.0",
    "express": "^4.17.2",
    "express-rate-limit": "^6.3.0",
    "sqlite": "^4.0.23"
  }
}
